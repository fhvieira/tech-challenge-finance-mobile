import { StyleSheet } from "react-native";

export const formValidationStyles = StyleSheet.create({
  errorInput: {
    borderColor: "#D7654B",
  },

  errorText: {
    color: "#D7654B",
    fontSize: 13,
    marginTop: -12,
    marginBottom: 12,
  },

  formErrorText: {
    color: "#D7654B",
    fontSize: 14,
    fontWeight: "600",
    marginBottom: 16,
    textAlign: "center",
  },
});
